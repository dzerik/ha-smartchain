{
  "config": {
    "step": {
      "user": {
        "title": "SmartChain configuration - select engine",
        "data": {
          "engine": "LLM Engine"
        },
        "data_description": {
          "engine": "The AI provider this SmartChain entry will use. Pick it once — each provider needs its own SmartChain entry, since credentials and available models differ between them."
        }
      },
      "gigachat": {
        "title": "GigaChat configuration",
        "data": {
          "api_key": "Auth data",
          "skip_validation": "Skip validation"
        },
        "data_description": {
          "api_key": "The Authorization key from your GigaChat project in the GigaChat console (a Base64 string, not your login and password). SmartChain uses it to request access tokens automatically.",
          "skip_validation": "Skips the test connection to GigaChat when you save this step. Turn it on only if GigaChat is not reachable yet (e.g. the network or certificate isn't ready) — you'll find out about a wrong key only once you actually use the agent, not now."
        }
      },
      "yandexgpt": {
        "title": "YandexGPT configuration",
        "data": {
          "api_key": "API Key",
          "folder_id": "Folder ID",
          "skip_validation": "Skip validation"
        },
        "data_description": {
          "api_key": "The API key created in Yandex Cloud for the service account that has access to YandexGPT. Used together with the Folder ID below to authenticate every request.",
          "folder_id": "The ID of the Yandex Cloud folder that owns the API key above. Found on the folder's page in the Yandex Cloud console — with the wrong one, YandexGPT will reject every request.",
          "skip_validation": "Skips the test connection to YandexGPT when you save this step. Turn it on only if the service is not reachable yet — a wrong key or folder ID will then only surface once you use the agent."
        }
      },
      "openai": {
        "title": "OpenAI configuration",
        "data": {
          "api_key": "API Key",
          "base_url": "API base URL",
          "skip_validation": "Skip validation"
        },
        "data_description": {
          "api_key": "Your OpenAI API key, created on the OpenAI platform. SmartChain sends it with every request to authenticate as you.",
          "base_url": "The API endpoint SmartChain sends requests to. Leave the default unless you're routing through a proxy or a self-hosted OpenAI-compatible gateway.",
          "skip_validation": "Skips the test connection to OpenAI when you save this step. Turn it on only if the endpoint is not reachable yet — a wrong key will then only surface once you use the agent."
        }
      },
      "ollama": {
        "title": "Ollama configuration",
        "data": {
          "base_url": "Base URL",
          "skip_validation": "Skip validation"
        },
        "data_description": {
          "base_url": "The address of your Ollama server, including port (e.g. http://localhost:11434). SmartChain sends every request here, so it must be reachable from Home Assistant.",
          "skip_validation": "Skips the test connection to Ollama when you save this step. Turn it on only if the server is not running yet or not reachable from Home Assistant at setup time."
        }
      },
      "deepseek": {
        "title": "DeepSeek configuration",
        "data": {
          "api_key": "API Key",
          "base_url": "API base URL",
          "skip_validation": "Skip validation"
        },
        "data_description": {
          "api_key": "Your DeepSeek API key, created on the DeepSeek platform. SmartChain sends it with every request to authenticate as you.",
          "base_url": "The API endpoint SmartChain sends requests to. Leave the default unless you're routing through a proxy or a self-hosted gateway.",
          "skip_validation": "Skips the test connection to DeepSeek when you save this step. Turn it on only if the endpoint is not reachable yet — a wrong key will then only surface once you use the agent."
        }
      },
      "anthropic": {
        "title": "Anthropic (Claude) configuration",
        "data": {
          "api_key": "API Key",
          "skip_validation": "Skip validation"
        },
        "data_description": {
          "api_key": "Your Anthropic API key, created in the Anthropic console. SmartChain sends it with every request to authenticate as you.",
          "skip_validation": "Skips the test connection to Anthropic when you save this step. Turn it on only if the endpoint is not reachable yet — a wrong key will then only surface once you use the agent."
        }
      },
      "openrouter": {
        "title": "OpenRouter configuration",
        "data": {
          "api_key": "API Key",
          "base_url": "API base URL",
          "skip_validation": "Skip validation"
        },
        "data_description": {
          "api_key": "Your OpenRouter API key, created on the OpenRouter dashboard. SmartChain sends it with every request to authenticate as you.",
          "base_url": "The API endpoint SmartChain sends requests to. Leave the default unless you're routing through a proxy.",
          "skip_validation": "Skips the test connection to OpenRouter when you save this step. Turn it on only if the endpoint is not reachable yet — a wrong key will then only surface once you use the agent."
        }
      },
      "groq": {
        "title": "Groq configuration",
        "data": {
          "api_key": "API Key",
          "base_url": "API base URL",
          "skip_validation": "Skip validation"
        },
        "data_description": {
          "api_key": "Your Groq API key, created on the Groq console. SmartChain sends it with every request to authenticate as you.",
          "base_url": "The API endpoint SmartChain sends requests to. Leave the default unless you're routing through a proxy.",
          "skip_validation": "Skips the test connection to Groq when you save this step. Turn it on only if the endpoint is not reachable yet — a wrong key will then only surface once you use the agent."
        }
      },
      "together": {
        "title": "Together configuration",
        "data": {
          "api_key": "API Key",
          "base_url": "API base URL",
          "skip_validation": "Skip validation"
        },
        "data_description": {
          "api_key": "Your Together AI API key, created on the Together dashboard. SmartChain sends it with every request to authenticate as you.",
          "base_url": "The API endpoint SmartChain sends requests to. Leave the default unless you're routing through a proxy.",
          "skip_validation": "Skips the test connection to Together when you save this step. Turn it on only if the endpoint is not reachable yet — a wrong key will then only surface once you use the agent."
        }
      },
      "lmstudio": {
        "title": "LM Studio configuration",
        "data": {
          "base_url": "Server URL",
          "api_key": "API Key (optional)",
          "skip_validation": "Skip validation"
        },
        "data_description": {
          "base_url": "The address of your LM Studio local server, including port (e.g. http://localhost:1234/v1). SmartChain sends every request here, so it must be reachable from Home Assistant.",
          "api_key": "Usually not needed for a local LM Studio server. Fill it in only if your server sits behind a proxy that requires one.",
          "skip_validation": "Skips the test connection to LM Studio when you save this step. Turn it on only if the server is not running yet or not reachable from Home Assistant at setup time."
        }
      },
      "llamacpp": {
        "title": "llama.cpp configuration",
        "data": {
          "base_url": "Server URL",
          "api_key": "API Key (optional)",
          "skip_validation": "Skip validation"
        },
        "data_description": {
          "base_url": "The address of your llama.cpp server, including port. SmartChain sends every request here, so it must be reachable from Home Assistant.",
          "api_key": "Usually not needed for a local llama.cpp server. Fill it in only if your server sits behind a proxy that requires one.",
          "skip_validation": "Skips the test connection to llama.cpp when you save this step. Turn it on only if the server is not running yet or not reachable from Home Assistant at setup time."
        }
      }
    },
    "abort": {
      "already_configured": "Already configured"
    },
    "error": {
      "cannot_connect": "Can not connect",
      "invalid_response": "Invalid response",
      "unknown": "Unknown error"
    }
  },
  "options": {
    "abort": {
      "no_connection_settings": "This provider has no connection settings. Models, prompts and tools belong to an agent — add or edit one under this entry."
    },
    "error": {
      "model_required": "Either Model or Custom Model required"
    },
    "step": {
      "settings": {
        "title": "Connection settings",
        "data": {
          "llm_hass_api": "Assist API (Control HA devices)",
          "prompt": "Prompt Template",
          "model": "Completion Model",
          "model_user": "Custom Model Name (leave empty to use from list above)",
          "temperature": "Temperature",
          "max_tokens": "Max Tokens",
          "profanity": "Profanity",
          "verify_ssl": "Verify SSL Certificates",
          "process_builtin_sentences": "Process HA Builtin Sentences",
          "chat_history": "Chat History",
          "dynamic_entity_context": "Dynamic entity context (send a compact map of the home plus what the message is about, instead of every entity)",
          "dynamic_context_preset": "Which entities the map covers",
          "dynamic_context_on_assist": "Also add matched entities when the Assist API is enabled",
          "allowed_tools": "Tools (everything this agent may call, built-in and custom)"
        },
        "data_description": {
          "llm_hass_api": "Which Home Assistant Assist API(s) this agent can use to see and control your devices. Selecting one lets the agent call services like turning on lights; leaving it empty means the agent can only talk, not act.",
          "prompt": "The system prompt sent to the model before every conversation — instructions on how the agent should behave, what tone to use, and what it knows about your home. Supports Jinja2 templates, so it can include live data such as the current time or entity states.",
          "model": "The model to use, picked from the list SmartChain fetched from the provider. Leave the Custom Model Name field below empty to use this selection.",
          "model_user": "Overrides the dropdown above with a model name typed by hand — for a model the provider offers but isn't in SmartChain's list, or a brand-new release it doesn't know about yet. Leave empty to use the dropdown selection instead.",
          "temperature": "Controls how predictable the model's replies are. Lower values (near 0) give more focused, repeatable answers; higher values (near 1) give more varied, creative ones.",
          "max_tokens": "The longest reply the model is allowed to generate, in tokens (roughly, word fragments). Leave empty to use the provider's own default; a low value can cut a reply off mid-sentence.",
          "profanity": "GigaChat-specific. When on, GigaChat filters profanity from its own replies; when off, replies are returned unfiltered.",
          "verify_ssl": "GigaChat-specific. When on, SmartChain verifies GigaChat's SSL certificate as usual. Turn it off only if you connect through a proxy with a self-signed certificate — doing so removes protection against a tampered connection.",
          "process_builtin_sentences": "When on and no Assist API is selected above, Home Assistant's own built-in sentence matching gets first try at your message (e.g. simple commands like \"turn off the kitchen light\"), and only messages it can't handle reach the LLM. Off sends every message straight to the model.",
          "chat_history": "When on, the agent remembers earlier messages in the same conversation, so follow-up questions work without repeating context. Off treats every message as a fresh conversation with no memory of what came before.",
          "dynamic_entity_context": "Controls how the agent learns about your entities. On (default), it gets a compact map of the whole home plus a handful of entities relevant to the current message — smaller, cheaper prompts, at the cost of occasionally missing an obscure entity the map or the match didn't surface. Off sends the full entity list on every turn, like before — larger, slower and pricier, but the agent always sees everything.",
          "dynamic_context_preset": "How much of the home the entity map and the per-message matches are allowed to cover, from minimal to paranoid — wider presets help the agent find more obscure entities but grow the prompt. Presence data (device trackers) is deliberately left out of every preset except maximal and paranoid, since it's the most sensitive data to hand to an outside model.",
          "dynamic_context_on_assist": "Adds the entities matched to your message onto Assist requests as well, not just plain conversation. Off by default because this block does not respect Home Assistant's own \"expose to Assist\" setting — turning it on can let the agent name entities you specifically withheld from Assist.",
          "allowed_tools": "The single list of everything this agent may call. It offers the six built-in tools alongside every custom tool you have written, so one place answers what this agent can do. \"All tools\" means every custom tool, including MCP tools by their prefixed name; a built-in is included only when its own name is selected. Select nothing to leave the agent with no tools at all. A tool added later is not granted to an agent that lists its tools explicitly."
        }
      }
    }
  },
  "config_subentries": {
    "conversation": {
      "entry_type": "Conversation agent",
      "initiate_flow": {
        "user": "Add conversation agent",
        "reconfigure": "Reconfigure conversation agent"
      },
      "step": {
        "user": {
          "title": "Add conversation agent",
          "data": {
            "model": "Completion Model",
            "model_user": "Custom Model Name (leave empty to use from list above)",
            "llm_hass_api": "Assist API (Control HA devices)",
            "prompt": "Prompt Template",
            "temperature": "Temperature",
            "max_tokens": "Max Tokens",
            "profanity": "Profanity",
            "verify_ssl": "Verify SSL Certificates",
            "process_builtin_sentences": "Process HA Builtin Sentences",
            "chat_history": "Chat History",
            "dynamic_entity_context": "Dynamic entity context (send a compact map of the home plus what the message is about, instead of every entity)",
            "dynamic_context_preset": "Which entities the map covers",
            "dynamic_context_on_assist": "Also add matched entities when the Assist API is enabled",
            "allowed_tools": "Tools (everything this agent may call, built-in and custom)"
          },
          "data_description": {
            "model": "The model to use, picked from the list SmartChain fetched from the provider. Leave the Custom Model Name field below empty to use this selection.",
            "model_user": "Overrides the dropdown above with a model name typed by hand — for a model the provider offers but isn't in SmartChain's list, or a brand-new release it doesn't know about yet. Leave empty to use the dropdown selection instead.",
            "llm_hass_api": "Which Home Assistant Assist API(s) this agent can use to see and control your devices. Selecting one lets the agent call services like turning on lights; leaving it empty means the agent can only talk, not act.",
            "prompt": "The system prompt sent to the model before every conversation — instructions on how the agent should behave, what tone to use, and what it knows about your home. Supports Jinja2 templates, so it can include live data such as the current time or entity states.",
            "temperature": "Controls how predictable the model's replies are. Lower values (near 0) give more focused, repeatable answers; higher values (near 1) give more varied, creative ones.",
            "max_tokens": "The longest reply the model is allowed to generate, in tokens (roughly, word fragments). Leave empty to use the provider's own default; a low value can cut a reply off mid-sentence.",
            "profanity": "GigaChat-specific. When on, GigaChat filters profanity from its own replies; when off, replies are returned unfiltered.",
            "verify_ssl": "GigaChat-specific. When on, SmartChain verifies GigaChat's SSL certificate as usual. Turn it off only if you connect through a proxy with a self-signed certificate — doing so removes protection against a tampered connection.",
            "process_builtin_sentences": "When on and no Assist API is selected above, Home Assistant's own built-in sentence matching gets first try at your message (e.g. simple commands like \"turn off the kitchen light\"), and only messages it can't handle reach the LLM. Off sends every message straight to the model.",
            "chat_history": "When on, the agent remembers earlier messages in the same conversation, so follow-up questions work without repeating context. Off treats every message as a fresh conversation with no memory of what came before.",
            "dynamic_entity_context": "Controls how the agent learns about your entities. On (default), it gets a compact map of the whole home plus a handful of entities relevant to the current message — smaller, cheaper prompts, at the cost of occasionally missing an obscure entity the map or the match didn't surface. Off sends the full entity list on every turn, like before — larger, slower and pricier, but the agent always sees everything.",
            "dynamic_context_preset": "How much of the home the entity map and the per-message matches are allowed to cover, from minimal to paranoid — wider presets help the agent find more obscure entities but grow the prompt. Presence data (device trackers) is deliberately left out of every preset except maximal and paranoid, since it's the most sensitive data to hand to an outside model.",
            "dynamic_context_on_assist": "Adds the entities matched to your message onto Assist requests as well, not just plain conversation. Off by default because this block does not respect Home Assistant's own \"expose to Assist\" setting — turning it on can let the agent name entities you specifically withheld from Assist.",
            "allowed_tools": "The single list of everything this agent may call. It offers the six built-in tools alongside every custom tool you have written, so one place answers what this agent can do. \"All tools\" means every custom tool, including MCP tools by their prefixed name; a built-in is included only when its own name is selected. Select nothing to leave the agent with no tools at all. A tool added later is not granted to an agent that lists its tools explicitly."
          }
        },
        "reconfigure": {
          "title": "Reconfigure conversation agent",
          "data": {
            "model": "Completion Model",
            "model_user": "Custom Model Name (leave empty to use from list above)",
            "llm_hass_api": "Assist API (Control HA devices)",
            "prompt": "Prompt Template",
            "temperature": "Temperature",
            "max_tokens": "Max Tokens",
            "profanity": "Profanity",
            "verify_ssl": "Verify SSL Certificates",
            "process_builtin_sentences": "Process HA Builtin Sentences",
            "chat_history": "Chat History",
            "dynamic_entity_context": "Dynamic entity context (send a compact map of the home plus what the message is about, instead of every entity)",
            "dynamic_context_preset": "Which entities the map covers",
            "dynamic_context_on_assist": "Also add matched entities when the Assist API is enabled",
            "allowed_tools": "Tools (everything this agent may call, built-in and custom)"
          },
          "data_description": {
            "model": "The model to use, picked from the list SmartChain fetched from the provider. Leave the Custom Model Name field below empty to use this selection.",
            "model_user": "Overrides the dropdown above with a model name typed by hand — for a model the provider offers but isn't in SmartChain's list, or a brand-new release it doesn't know about yet. Leave empty to use the dropdown selection instead.",
            "llm_hass_api": "Which Home Assistant Assist API(s) this agent can use to see and control your devices. Selecting one lets the agent call services like turning on lights; leaving it empty means the agent can only talk, not act.",
            "prompt": "The system prompt sent to the model before every conversation — instructions on how the agent should behave, what tone to use, and what it knows about your home. Supports Jinja2 templates, so it can include live data such as the current time or entity states.",
            "temperature": "Controls how predictable the model's replies are. Lower values (near 0) give more focused, repeatable answers; higher values (near 1) give more varied, creative ones.",
            "max_tokens": "The longest reply the model is allowed to generate, in tokens (roughly, word fragments). Leave empty to use the provider's own default; a low value can cut a reply off mid-sentence.",
            "profanity": "GigaChat-specific. When on, GigaChat filters profanity from its own replies; when off, replies are returned unfiltered.",
            "verify_ssl": "GigaChat-specific. When on, SmartChain verifies GigaChat's SSL certificate as usual. Turn it off only if you connect through a proxy with a self-signed certificate — doing so removes protection against a tampered connection.",
            "process_builtin_sentences": "When on and no Assist API is selected above, Home Assistant's own built-in sentence matching gets first try at your message (e.g. simple commands like \"turn off the kitchen light\"), and only messages it can't handle reach the LLM. Off sends every message straight to the model.",
            "chat_history": "When on, the agent remembers earlier messages in the same conversation, so follow-up questions work without repeating context. Off treats every message as a fresh conversation with no memory of what came before.",
            "dynamic_entity_context": "Controls how the agent learns about your entities. On (default), it gets a compact map of the whole home plus a handful of entities relevant to the current message — smaller, cheaper prompts, at the cost of occasionally missing an obscure entity the map or the match didn't surface. Off sends the full entity list on every turn, like before — larger, slower and pricier, but the agent always sees everything.",
            "dynamic_context_preset": "How much of the home the entity map and the per-message matches are allowed to cover, from minimal to paranoid — wider presets help the agent find more obscure entities but grow the prompt. Presence data (device trackers) is deliberately left out of every preset except maximal and paranoid, since it's the most sensitive data to hand to an outside model.",
            "dynamic_context_on_assist": "Adds the entities matched to your message onto Assist requests as well, not just plain conversation. Off by default because this block does not respect Home Assistant's own \"expose to Assist\" setting — turning it on can let the agent name entities you specifically withheld from Assist.",
            "allowed_tools": "The single list of everything this agent may call. It offers the six built-in tools alongside every custom tool you have written, so one place answers what this agent can do. \"All tools\" means every custom tool, including MCP tools by their prefixed name; a built-in is included only when its own name is selected. Select nothing to leave the agent with no tools at all. A tool added later is not granted to an agent that lists its tools explicitly."
          }
        }
      },
      "error": {
        "model_required": "Either Model or Custom Model required"
      }
    },
    "embeddings": {
      "entry_type": "Embeddings binding",
      "initiate_flow": {
        "user": "Add embeddings binding",
        "reconfigure": "Reconfigure embeddings binding"
      },
      "step": {
        "user": {
          "title": "Add embeddings binding",
          "data": {
            "name": "Name",
            "model": "Embedding model",
            "model_user": "Custom model name (leave empty to use the list above)"
          },
          "data_description": {
            "name": "A name for this embeddings binding, used to refer to it when connecting a memory store to it. Must be unique among your embeddings bindings.",
            "model": "The embedding model to use, picked from the list SmartChain fetched from the provider. Leave Custom model name below empty to use this selection. If you change it later, every memory store and entity index bound to this binding needs to be re-indexed.",
            "model_user": "Overrides the dropdown above with an embedding model name typed by hand — for a model the provider offers but isn't in SmartChain's list. Leave empty to use the dropdown selection instead."
          }
        },
        "reconfigure": {
          "title": "Reconfigure embeddings binding",
          "data": {
            "name": "Name",
            "model": "Embedding model",
            "model_user": "Custom model name (leave empty to use the list above)"
          },
          "data_description": {
            "name": "A name for this embeddings binding, used to refer to it when connecting a memory store to it. Must be unique among your embeddings bindings.",
            "model": "The embedding model to use, picked from the list SmartChain fetched from the provider. Leave Custom model name below empty to use this selection. If you change it later, every memory store and entity index bound to this binding needs to be re-indexed.",
            "model_user": "Overrides the dropdown above with an embedding model name typed by hand — for a model the provider offers but isn't in SmartChain's list. Leave empty to use the dropdown selection instead."
          }
        }
      },
      "error": {
        "model_required": "Select a model or enter a custom name"
      }
    },
    "memory_store": {
      "entry_type": "Memory store",
      "initiate_flow": {
        "user": "Add memory store",
        "reconfigure": "Reconfigure memory store"
      },
      "step": {
        "user": {
          "title": "Add memory store",
          "description": "The name, the embeddings binding and the backend. The last two answers decide what the next step asks for.",
          "data": {
            "name": "Store name",
            "embeddings": "Embeddings binding",
            "description": "Description (shown to the model)",
            "backend_type": "Vector backend",
            "source_type": "What this store holds"
          },
          "data_description": {
            "name": "Lowercase letters, digits and underscores, starting with a letter or underscore. This is the name search_memory and smartchain.clear_memory refer to, and for a file-based backend it is also the database filename — renaming a store points it at a fresh, empty one.",
            "embeddings": "Which embeddings binding turns text into vectors for this store. Stores resolve a binding by its title, so a title used twice resolves to nothing at all.",
            "description": "One line telling the model what lives in this store, so it can choose between several. Shown in the search_memory tool description.",
            "backend_type": "Where the vectors are kept. sqlite_numpy needs nothing installed and is the right default; sqlite_vec is faster on large stores; pgvector and qdrant put the vectors on a server you already run.",
            "source_type": "'none' is a memory store: conversation turns go in, search_memory reads them back. 'entities' turns it into a semantic index of your home for search_entities — the two cannot be combined, because retention would delete the index by age."
          }
        },
        "reconfigure": {
          "title": "Reconfigure memory store",
          "description": "The name, the embeddings binding and the backend. The last two answers decide what the next step asks for.",
          "data": {
            "name": "Store name",
            "embeddings": "Embeddings binding",
            "description": "Description (shown to the model)",
            "backend_type": "Vector backend",
            "source_type": "What this store holds"
          },
          "data_description": {
            "name": "Lowercase letters, digits and underscores, starting with a letter or underscore. This is the name search_memory and smartchain.clear_memory refer to, and for a file-based backend it is also the database filename — renaming a store points it at a fresh, empty one.",
            "embeddings": "Which embeddings binding turns text into vectors for this store. Stores resolve a binding by its title, so a title used twice resolves to nothing at all.",
            "description": "One line telling the model what lives in this store, so it can choose between several. Shown in the search_memory tool description.",
            "backend_type": "Where the vectors are kept. sqlite_numpy needs nothing installed and is the right default; sqlite_vec is faster on large stores; pgvector and qdrant put the vectors on a server you already run.",
            "source_type": "'none' is a memory store: conversation turns go in, search_memory reads them back. 'entities' turns it into a semantic index of your home for search_entities — the two cannot be combined, because retention would delete the index by age."
          }
        },
        "details": {
          "title": "Store settings",
          "description": "The settings that depend on the backend and on what the store holds.",
          "data": {
            "path": "Database file (leave empty for the default)",
            "dsn": "PostgreSQL connection string",
            "table": "Table name",
            "url": "Qdrant server URL",
            "api_key": "Qdrant API key",
            "collection": "Collection name",
            "verify_ssl": "Verify the server's SSL certificate",
            "retention_days": "Delete memories older than (days, 0 = never)",
            "ingest_conversation": "Store conversation turns automatically",
            "preset": "Which entities the index covers",
            "index_states": "Also record each entity's current state",
            "include": "Always include (a domain, or a full entity id)",
            "exclude": "Never include (a domain, or a full entity id)"
          },
          "data_description": {
            "path": "An absolute path to the SQLite file. Leave empty to keep it in Home Assistant's .storage directory, named after the store.",
            "dsn": "postgresql://user:password@host/database. Stored with Home Assistant's other credentials and never shown again — leave empty when editing to keep the one already saved.",
            "table": "Lowercase letters, digits and underscores. It is written straight into SQL and cannot be quoted, which is why the shape is restricted.",
            "url": "https://host:6333 — the base URL of the Qdrant server.",
            "api_key": "Never shown again once saved — leave empty when editing to keep the one already stored. Leave empty on a server that needs no key.",
            "collection": "Lowercase letters, digits and underscores. It becomes part of a URL path and cannot be escaped, which is why the shape is restricted.",
            "verify_ssl": "Turn off only for a server with a self-signed certificate; doing so removes protection against an intercepted connection.",
            "retention_days": "Older memories are deleted once a day. 0 keeps everything forever, which on a busy install grows without limit.",
            "ingest_conversation": "When on, every exchange with an agent is embedded into this store so the agent can recall it later. Turn off for a store you fill yourself.",
            "preset": "How much of the home the index covers, smallest to largest. 'optimal' deliberately leaves out device_tracker: presence data is the most sensitive domain and indexing sends it to the embeddings provider.",
            "index_states": "Records the current state alongside each entity, refreshed periodically. It makes filtering possible; it does not make states searchable by meaning.",
            "include": "Added on top of the preset. Accepts a bare domain (sensor) or a full entity id (sensor.kitchen_temperature).",
            "exclude": "Applied last and wins over both the preset and the include list."
          }
        }
      },
      "error": {
        "invalid_name": "A store name must be lowercase letters, digits and underscores, starting with a letter or underscore.",
        "name_taken": "Another memory store already uses this name.",
        "embeddings_required": "Pick which embeddings binding this store uses.",
        "embeddings_unknown": "No embeddings binding has that name.",
        "embeddings_ambiguous": "That embeddings title is claimed by more than one binding, so it resolves to nothing. Rename one of them first.",
        "dsn_required": "The pgvector backend needs a connection string.",
        "url_required": "The qdrant backend needs a server URL.",
        "invalid_identifier": "Must be lowercase letters, digits and underscores, starting with a letter or underscore."
      }
    },
    "tool": {
      "entry_type": "Tool",
      "initiate_flow": {
        "user": "Add tool",
        "reconfigure": "Reconfigure tool"
      },
      "step": {
        "user": {
          "title": "Add tool",
          "description": "The name, what the tool does, and what happens when the model calls it. The last two answers decide what the next step asks for.",
          "data": {
            "name": "Tool name",
            "description": "What the tool does",
            "enabled": "Enabled",
            "action_type": "What happens when the model calls it",
            "params_mode": "How the arguments are described"
          },
          "data_description": {
            "name": "Lowercase letters, digits and underscores, starting with a letter or underscore. This is the name the model calls, and the name an agent's “Allowed tools” setting refers to. The six built-in tool names are reserved.",
            "description": "The only thing the model reads when deciding whether to call this tool. Say what it does and when to use it, not how it is implemented.",
            "enabled": "Turn a tool off without deleting it. A disabled tool is not offered to any agent, and does not reserve its name against another tool.",
            "action_type": "service calls a Home Assistant action, template renders Jinja and returns the text, rest calls an HTTP endpoint, script runs a script. Changing this changes which fields the rest of the form asks for.",
            "params_mode": "simple is one row per argument and covers almost every tool. advanced is a raw JSON Schema box, for shapes rows cannot express — anyOf, nested objects, arrays."
          }
        },
        "reconfigure": {
          "title": "Reconfigure tool",
          "description": "The name, what the tool does, and what happens when the model calls it. The last two answers decide what the next step asks for.",
          "data": {
            "name": "Tool name",
            "description": "What the tool does",
            "enabled": "Enabled",
            "action_type": "What happens when the model calls it",
            "params_mode": "How the arguments are described"
          },
          "data_description": {
            "name": "Lowercase letters, digits and underscores, starting with a letter or underscore. This is the name the model calls, and the name an agent's “Allowed tools” setting refers to. The six built-in tool names are reserved.",
            "description": "The only thing the model reads when deciding whether to call this tool. Say what it does and when to use it, not how it is implemented.",
            "enabled": "Turn a tool off without deleting it. A disabled tool is not offered to any agent, and does not reserve its name against another tool.",
            "action_type": "service calls a Home Assistant action, template renders Jinja and returns the text, rest calls an HTTP endpoint, script runs a script. Changing this changes which fields the rest of the form asks for.",
            "params_mode": "simple is one row per argument and covers almost every tool. advanced is a raw JSON Schema box, for shapes rows cannot express — anyOf, nested objects, arrays."
          }
        },
        "details": {
          "title": "Tool details",
          "description": "The arguments the model passes, and the settings of the action type chosen on the previous step.",
          "data": {
            "params_rows": "Arguments",
            "params_json": "Arguments (JSON Schema)",
            "service": "Home Assistant action",
            "target": "Target",
            "service_data": "Action data",
            "response": "Return the action's response to the model",
            "value_template": "Template",
            "method": "HTTP method",
            "url": "URL",
            "headers": "Headers",
            "payload": "JSON body",
            "timeout": "Timeout (seconds)",
            "response_format": "Response format",
            "script": "Script",
            "variables": "Script variables"
          },
          "data_description": {
            "params_rows": "The arguments the model passes when it calls this tool. Reference one inside a template by its bare name: {{ city }}, not {{ args.city }}. Leave the list empty for a tool that takes no arguments.",
            "params_json": "A JSON Schema object describing the call's arguments — an object with \"type\": \"object\" and a \"properties\" map. Validated before it is saved, and again against every call the model makes.",
            "service": "The action to call, e.g. light.turn_on. Picked from the actions registered right now; an action from an integration that is not loaded can still be typed in.",
            "target": "Which entities, devices or areas the action runs on.",
            "service_data": "Extra data passed to the action. Values may contain {{ }} templates referencing the tool's arguments.",
            "response": "On for an action that returns data the model should read (a forecast, a snapshot analysis). Off returns just “OK”. The action must support returning a response.",
            "value_template": "Rendered with the tool's arguments in scope; the rendered text is what the model gets back.",
            "method": "The HTTP method used for the request.",
            "url": "May contain {{ }} templates referencing the tool's arguments.",
            "headers": "Sent with the request; values may template too. Header values are never shown again once saved — leave one blank to keep the stored value, or type a new one to replace it.",
            "payload": "JSON request body, for POST and PUT. Leave empty to send no body at all, which is not the same as sending an empty object.",
            "timeout": "How long to wait for the endpoint, 1–120 seconds.",
            "response_format": "json parses the response and hands the model the parsed data; text hands back the raw body.",
            "script": "The script this tool runs.",
            "variables": "Passed to the script as variables; values may contain {{ }} templates referencing the tool's arguments."
          }
        }
      },
      "error": {
        "invalid_name": "A tool name must be lowercase letters, digits and underscores, starting with a letter or underscore.",
        "reserved_name": "That name belongs to a built-in tool. A custom tool cannot replace a built-in — pick another name.",
        "name_taken": "Another tool already uses this name.",
        "description_required": "Describe what the tool does. This is the only thing the model reads when deciding whether to call it.",
        "invalid_parameters_json": "The arguments box does not contain valid JSON.",
        "invalid_parameters": "Arguments must be a JSON Schema object: an object with \"type\": \"object\" and a \"properties\" map.",
        "invalid_parameter_name": "An argument name must be letters, digits and underscores, starting with a letter or underscore.",
        "duplicate_parameter": "Two arguments have the same name.",
        "parameter_type_required": "Every argument needs a type.",
        "service_required": "Pick the Home Assistant action this tool calls.",
        "invalid_service": "An action is written as domain.action, e.g. light.turn_on.",
        "value_template_required": "Write the template this tool renders.",
        "url_required": "The rest action needs a URL.",
        "script_required": "Pick the script this tool runs.",
        "invalid_script": "A script action targets a script.* entity.",
        "invalid_action": "The action is not valid for its type. The detail is in the Home Assistant log — it is withheld here because an action can carry a credential."
      }
    }
  }
}
